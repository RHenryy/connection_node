extends layouts/frontend

include includes/_mixins

block head
  //- Inject here JS and styles for this specific page

block content
  .ui.middle.aligned.center.aligned.grid
    .sixteen.wide.ten.wide.tablet.seven.wide.computer.column
      if flashMessage 
        +message(flashMessageType) #{flashMessage}
      if user
        h1 Bienvenue #{user.firstName} #{user.lastName} !
      else
        h1 Vous devez vous connecter pour avoir accès au site.
        p 
          a(href='/login') Connexion
        p 
          a(href='/register') Inscription
