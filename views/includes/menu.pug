//- Top black menu bar
.ui.inverted.menu
  .left.menu
    a.item(href='/dashboard')
      i.home.icon
      | Dashboard

  .right.menu
    if !user
      a.item(href='/register' class='active')
        i.sign-in.icon
        | Inscription
    if !user
      a.item(href='/login')
        i.sign-in.icon
        | Connexion
    if user
      span.item
        i.user.icon
        | #{user.firstName} #{user.lastName}
    if user
      a.item(href='/logout')
        i.sign-out.icon
        | Déconnexion